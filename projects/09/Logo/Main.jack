class Main {
    function void main() {
        var Logo logo;
        var int Fn;
        do MathUtil.init();
        let logo = Logo.new();
        do logo.run();
        do logo.dispose();
        return;
    }
}
